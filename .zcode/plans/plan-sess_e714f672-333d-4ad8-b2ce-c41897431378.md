Update /var/www/viravach/AGENTS.md (edit, not rewrite — the file is already accurate and the appended laravel-boost-guidelines block stays untouched).

Add two new sections before "## Testing gotchas":

1. **## Tickets & support chats** (brief): Ticket model on musonza/chat — ticket = `chat_conversations` row with data.type=ticket (NOT makeDirect; staff visibility comes from the Ticket row, not participation). Shared staff queue by role (support/admin/super_admin). Ticket refs `TICKET-YYYYMMDD-NNNN` with UniqueConstraintViolationException retry. Customer reply reopens a closed ticket. Dashboard routes: tickets + support-chats on the app host.

2. **## WordPress auto-content (Virawp, in progress, uncommitted)**:
   - Purpose: monthly AI-generated articles published to the company's own WordPress site. State per article in `WordPressContentPost` (WordPressPostStatus queued/generating/published/failed).
   - Services in `app/Services/WordPress/` (connection, generation, publishing — result/failure-reason objects pattern), jobs in `app/Jobs/WordPress/` extending `AbstractWordPressPostJob`, trend discovery in `app/Services/Trends/` (GoogleTrendsService).
   - Connection settings on `companies` (wp url/credentials, WordPressConnectionStatus, SeoPlugin enum yoast/rank_math decides which SEO meta fields the publisher writes; `wp_seo_meta_writable` flag).
   - Quota: `WordPressContentQuota` (feature `virawp-monthly-contents`) counts this calendar month's rows — never uses the package's recordFeatureUsage (rolling window); limit read live via `PlanFeature::value()` (features are stored plan-slug-prefixed: `{plan-slug}-{key}`) so admin plan edits apply instantly.
   - Entry point: dashboard `⚡wordpress-content` page + WordPressConnectionTest/WordPressContentPageTest in tests/Feature/Dashboard/.
   - Note: migrations exist but are NOT yet applied to Postgres (same pattern as earlier features — user runs migrate).

No other changes. After the edit, no pint/test run needed (markdown only).