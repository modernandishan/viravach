All files already located and read; plan mode blocked the edits, so here is exactly what will be done.

**Files (both confirmed by reading them):**
- Wizard: `resources/views/pages/dashboard/⚡create-company.blade.php`
- Test: `tests/Feature/CreateCompanyPageTest.php`

**1. Wizard rule (line 187, rulesForStep(4)):**
- Before: `'logo' => ['nullable', 'image', 'mimes:jpg,jpeg,png,webp', 'max:4096'],`
- After: `'logo' => ['required', 'image', 'mimes:jpg,jpeg,png,webp', 'max:4096'],`

**2. Step 4 finding — createCompany() (line 302) currently only validates step 5:** `$this->validate($this->rulesForStep(5));`. Step 4 rules (logo) would be bypassable if the UI step is skipped. Fix: change to `$this->validate(array_merge($this->rulesForStep(4), $this->rulesForStep(5)));` (line 302 of the wizard).

**3. Edit-company:** will not be opened or modified — logo stays nullable there.

**4. Test fixes (minimum edits, same file only):**
- Add imports: `use Illuminate\Http\UploadedFile;` and `use Illuminate\Support\Facades\Storage;` (same pattern SettingsIntroVideoTest uses).
- In `submitWizard()` helper: add `Storage::fake('s3');` before the Livewire chain, and insert `->set('logo', UploadedFile::fake()->image('logo.png'))` between the two step-4 `nextStep` calls.
- Same insertion in the inline wizard walks that must still succeed: test_it_creates_a_company_and_auto_attaches_the_free_plan, test_a_successful_create_stores_the_brief_and_its_locale, and the three website-normalization tests (set logo after reaching step 4, before the nextStep/createCompany that now validates it). The "garbage website" test also gets the logo set so it keeps testing the website rule in isolation.
- No new test methods, no new assertions.

**5. Run:** `docker exec viravach_app php artisan test --compact tests/Feature/CreateCompanyPageTest.php` (the project's PHPUnit single-file command per AGENTS.md), then `vendor/bin/pint --dirty` in the container. No migration, no git commit — changes left uncommitted for review.