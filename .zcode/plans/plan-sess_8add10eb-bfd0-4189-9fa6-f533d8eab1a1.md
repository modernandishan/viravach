Leave /var/www/viravach/AGENTS.md unchanged. The file already exists, is comprehensive, and accurately documents everything a future ZCode agent needs that the generic Laravel Boost guidelines don't cover: Docker-only PHP environment, host-split routing (no routes/web.php), two frontends (Livewire 4 public site + Filament v5 admin), localization setup (mcamara/laravel-localization with no URL prefix for en), the Company draft/snapshot publication model, the AI content pipeline, subscriptions, ViraBot AI chat, tickets/support chats, WordPress auto-content (ViraWP), testing gotchas (PHPUnit only, sqlite vs Postgres divergence), homepage dependency on the `pages` slug='/' row, single-h1 SEO rule, workspace skills, command quick reference, and verification flow. Recent drift points (intro video, ViraWP rounds, dashboard rebrand) are tracked in auto-memory files rather than AGENTS.md, so the file stays short and stable.